import React,{useMemo,useState}from'react';
import{createRoot}from'react-dom/client';
import{Activity,AlertTriangle,BarChart3,Bell,Bot,CheckCircle2,ChevronRight,FileText,Menu,Search,ShieldCheck,Upload}from'lucide-react';
import'./index.css';
const API_BASE=(import.meta.env.VITE_API_BASE_URL||'https://v-shiroya-policy.onrender.com').trim().replace(/\/$/,'');
type Policy={id:number;policyNumber:string;ownerName:string;providerCompany:string;policyType:string;premiumAmount:number;policyStatus:string;endDate:string};
type ScanResult={success?:boolean;fileName:string;extraction?:any;error?:string;status?:'queued'|'scanning'|'done'|'failed';[key:string]:any};
const demoPolicies:Policy[]=[];
async function analyze(file:File,instruction='Analyze this insurance policy document, classify the exact policy type, and return structured JSON.'){const base64=await new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result).split('base64,')[1]||'');r.onerror=()=>reject(new Error('Could not read the selected file.'));r.readAsDataURL(file)});const res=await fetch(`${API_BASE}/api/analyze-policy`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({fileData:base64,fileName:file.name,mimeType:file.type||'application/pdf',instruction})});const text=await res.text();let payload:any={};try{payload=text?JSON.parse(text):{};}catch{payload={details:text};}if(!res.ok)throw new Error(payload?.details||payload?.error||`Request failed (${res.status})`);if(!payload?.extraction)throw new Error('The AI completed the request but returned no extraction result.');return payload;}
async function analyzeMany(files:File[],onResult:(index:number,result:ScanResult)=>void,onProgress:(done:number)=>void){let next=0,done=0;const workers=Array.from({length:Math.min(3,files.length)},async()=>{while(true){const i=next++;if(i>=files.length)return;onResult(i,{fileName:files[i].name,status:'scanning'});try{const payload=await analyze(files[i]);onResult(i,{success:true,status:'done',fileName:files[i].name,...payload});}catch(e:any){onResult(i,{success:false,status:'failed',fileName:files[i].name,error:e?.message||'Policy analysis failed'});}done++;onProgress(done);}});await Promise.all(workers);}
function toPolicy(result:ScanResult,index:number):Policy|null{if(!result?.success||!result.extraction)return null;const x=result.extraction;return{id:Date.now()+index,policyNumber:x.policyNumber||result.fileName,ownerName:x.ownerName||x.insuredPerson||'Not detected',providerCompany:x.providerCompany||x.detectedInsurer||'Not detected',policyType:x.policyType||'General Insurance',premiumAmount:Number(x.premiumAmount)||0,policyStatus:x.policyStatus||'ACTIVE',endDate:x.endDate||x.maturityDate||''};}
function Sidebar({tab,setTab,collapsed,setCollapsed}:{tab:string;setTab:(s:string)=>void;collapsed:boolean;setCollapsed:(b:boolean)=>void}){const items=[['dashboard','▦','Dashboard'],['scan','✦','Scan & Extract Policy'],['policies','⌕','Policy Database'],['clients','♙','Clients CRM'],['renewals','⚠','Renewals & Expiry'],['commissions','◉','Commissions'],['claims','➤','Claim Intimation'],['security','▣','IRDAI & Security'],['reports','▤','Reports & Analytics'],['settings','⚙','Settings']];return <aside className={`sidebar ${collapsed?'collapsed':''}`}><div className="brand"><div className="brandMark">VS</div>{!collapsed&&<><b>V SHIROYA</b><button onClick={()=>setCollapsed(true)}>‹</button></>}</div><nav>{items.map(([id,icon,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}><span>{icon}</span>{!collapsed&&<>{label}{id==='scan'&&<small>AI</small>}{id==='renewals'&&<small className="warn">3</small>}</>}</button>)}</nav><div className="sidebarUser"><div className="avatar">VS</div>{!collapsed&&<div><b>VIJAY SHIROYA</b><span>Insurance & Financials</span></div>}</div></aside>}
function App(){const[tab,setTab]=useState('dashboard'),[collapsed,setCollapsed]=useState(false),[search,setSearch]=useState(''),[files,setFiles]=useState<File[]>([]),[busy,setBusy]=useState(false),[results,setResults]=useState<ScanResult[]>([]),[error,setError]=useState(''),[progress,setProgress]=useState(0);const scannedPolicies=useMemo(()=>results.map(toPolicy).filter(Boolean) as Policy[],[results]);const policies=useMemo(()=>[...scannedPolicies,...demoPolicies],[scannedPolicies]);const filtered=policies.filter(p=>Object.values(p).join(' ').toLowerCase().includes(search.toLowerCase()));const runAnalysis=async()=>{if(!files.length)return;setBusy(true);setError('');setProgress(0);setResults(files.map(f=>({fileName:f.name,status:'queued'})));try{await analyzeMany(files,(index,result)=>setResults(prev=>{const next=prev.length===files.length?[...prev]:files.map(f=>({fileName:f.name,status:'queued'}));next[index]=result;return next;}),setProgress);const failedCount=results.filter(x=>x.status==='failed').length;if(failedCount)setError(`${failedCount} file(s) failed, but every uploaded file remains visible with its status.`);}catch(e:any){setError(e?.message||'Policy analysis failed');}finally{setBusy(false)}};return <div className="app"><Sidebar tab={tab} setTab={setTab} collapsed={collapsed} setCollapsed={setCollapsed}/><main className="main"><header className="topbar"><button className="mobileMenu"><Menu/></button><div className="search"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search policy number, client, provider..."/></div><div className="topActions"><button><Bot size={16}/> AI Assistant</button><button><Bell size={16}/> Alerts</button><span className="profile">VS&nbsp; VIJAY SHIROYA</span></div></header><section className="content"><div className="eyebrow">CHARTERED ACCOUNTANT WORKSPACE</div><div className="titleRow"><div><h1>Policy Portfolio Overview</h1><p>Real-time analytics for uploaded and scanned insurance policies.</p></div><button className="primary" onClick={()=>setTab('scan')}><Activity size={16}/> Analyze New Policy</button></div><div className="alert"><div><b>{busy?`${progress} of ${files.length} policies scanned`:`${results.filter(x=>x.success).length} policies scanned successfully`}</b><span>{busy?'Scanning continues even if you open another page in this website.':'Dashboard totals update automatically from your latest AI scans.'}</span></div><ChevronRight/></div>{tab==='dashboard'&&<Dashboard policies={filtered} uploadCount={files.length} scannedCount={results.filter(x=>x.success).length} busy={busy} progress={progress}/>} {tab==='policies'&&<PolicyTable policies={filtered}/>} {tab==='scan'&&<Scanner files={files} setFiles={setFiles} busy={busy} run={runAnalysis} results={results} error={error} progress={progress}/>} {['clients','renewals','commissions','claims','security','reports','settings'].includes(tab)&&<Placeholder tab={tab}/>}</section></main></div>}
function Dashboard({policies,uploadCount,scannedCount,busy,progress}:{policies:Policy[];uploadCount:number;scannedCount:number;busy:boolean;progress:number}){const active=policies.filter(p=>p.policyStatus==='ACTIVE').length;const expiring=policies.filter(p=>p.policyStatus==='EXPIRING SOON').length;const premium=policies.reduce((n,p)=>n+(Number(p.premiumAmount)||0),0);const cards=[['TOTAL UPLOADED',String(uploadCount),busy?`${progress} currently processed`:`${scannedCount} scanned successfully`,'green'],['ACTIVE POLICIES',String(active),policies.length?`${Math.round(active/policies.length*100)}% active`:'No scan data yet','green'],['PREMIUM VALUE',premium?`₹${premium.toLocaleString('en-IN')}`:'₹0','From scanned policy results','green'],['EXPIRING SOON',String(expiring),expiring?'Action required':'No scanned expiry alerts','orange']];return <><div className="cards">{cards.map(([a,b,c,d])=><div className="card" key={a}><span>{a}</span><strong>{b}</strong><em className={d}>{c}</em></div>)}</div><div className="grid"><div className="panel chart"><div className="panelHead"><b>Premium Portfolio</b><span>Live scan data</span></div><div className="bars">{policies.slice(-12).map((p,i)=><div key={i} style={{height:`${Math.max(12,Math.min(100,(Number(p.premiumAmount)||0)/Math.max(1,...policies.map(x=>Number(x.premiumAmount)||0))*100))}%`}}><small>{i+1}</small></div>)}</div></div><div className="panel"><div className="panelHead"><b>Quick Actions</b></div><div className="quick"><button><Upload/><b>Scan & Extract</b><span>Upload one or many policies</span></button><button><FileText/><b>Policy Database</b><span>{scannedCount} AI-scanned policies</span></button><button><AlertTriangle/><b>Renewals</b><span>{expiring} require attention</span></button><button><BarChart3/><b>Reports</b><span>Live scan analytics</span></button></div></div></div><div className="panel recent"><div className="panelHead"><b>Latest Scanned Policies</b></div><PolicyTable policies={policies.slice(-20)}/></div></>}
function PolicyTable({policies}:{policies:Policy[]}){return <div className="tableWrap"><table><thead><tr><th>Policy</th><th>Client</th><th>Provider</th><th>Type</th><th>Premium</th><th>Status</th></tr></thead><tbody>{policies.length?policies.map(p=><tr key={p.id}><td><b>{p.policyNumber}</b></td><td>{p.ownerName}</td><td>{p.providerCompany}</td><td>{p.policyType}</td><td>₹{p.premiumAmount.toLocaleString('en-IN')}</td><td><span className={`status ${p.policyStatus.replaceAll(' ','-').toLowerCase()}`}>{p.policyStatus}</span></td></tr>):<tr><td colSpan={6}>No policies scanned yet.</td></tr>}</tbody></table></div>}
function Scanner({files,setFiles,busy,run,results,error,progress}:{files:File[];setFiles:(f:File[])=>void;busy:boolean;run:()=>void;results:ScanResult[];error:string;progress:number}){return <div className="scanLayout"><div className="panel uploadPanel"><div className="panelHead"><b>Scan & Extract Policy</b><span>Powered by OpenRouter via Render</span></div><label className="drop"><Upload size={34}/><b>{files.length?`${files.length} file${files.length>1?'s':''} selected`:'Drop policy PDF files here'}</b><span>PDF or image files · Fast bulk scanning supported</span><input type="file" multiple accept="application/pdf,image/*" onChange={e=>setFiles(Array.from(e.target.files||[]))}/></label><button className="primary full" disabled={!files.length||busy} onClick={run}>{busy?`Analyzing ${progress}/${files.length} policies…`:`Analyze ${files.length||''} Policy${files.length===1?'':'ies'}`}</button>{busy&&<div className="loading"><Activity/><b>Scanning continues in the background</b><span>You can safely open Dashboard or other pages and return here without losing progress.</span></div>}{error&&<div className="error">{error}</div>}</div><div className="panel resultPanel"><div className="panelHead"><b>Extraction Results {results.length?`(${results.filter(x=>x.success).length}/${files.length})`:''}</b>{results.some(x=>x.success)&&<CheckCircle2 className="ok"/>}</div>{results.length?<div>{results.map((r,i)=>{const extraction=r?.extraction||r;return <div key={`${r.fileName}-${i}`} style={{marginBottom:16}}><b>{i+1}. {r.fileName}</b>{r.status==='queued'||r.status==='scanning'?<div className="loading"><Activity/><b>{r.status==='queued'?'Waiting to scan…':'AI is scanning this policy…'}</b></div>:r.success===false?<div className="error">{r.error||'Analysis failed'}</div>:<pre>{JSON.stringify(extraction,null,2)}</pre>}</div>})}</div>:<div className="empty"><FileText size={40}/><span>Upload one or more policies to view every extracted result.</span></div>}</div></div>}
function Placeholder({tab}:{tab:string}){return <div className="panel placeholder"><ShieldCheck size={44}/><h2>{tab.replace(/^./,c=>c.toUpperCase())}</h2><p>This workspace remains connected to the Render backend. Existing navigation is preserved.</p></div>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);